#!/bin/sh
set -e
if [ "$1" = "remove" ] || [ "$1" = "deconfigure" ]; then
    if command -v systemctl >/dev/null 2>&1; then
        systemctl stop main-serve.service 2>/dev/null || true
        systemctl disable main-serve.service 2>/dev/null || true
    fi
fi
